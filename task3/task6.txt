// 6. Составьте схему нормального алгоритма Маркова
// для сложения двух натуральных чисел в двоичном позиционным коде
// в алфавите из символов "0" и "1", записанных старшими разрядами влево.
// Между слагаемыми в исходном слове стоит символ "+".
// Результат тоже должен быть словом, представляющим число в таком же
// двоичном коде.
// Например, для вычисления суммы 6 и 4 исходное слово будет "110+100".
// Результатом будет слово "1010", обозначающее число 10.
// Приведите протокол вычисления для слова "101+11".

// После выполнения участков ниже, выполнится это
&I -> 1&
&O -> 0&
& -> x

// Скопировал из bin2un
// Сначала переводим оба числа в унарные
|d -> d||
d ->
|0 -> |d
|1 -> |d|
1 -> |
0 ->

// потом удаляем плюс между ними
+ ->

// Скопировал из UN2BIN и измнил, чтобы не вызывать bin2un

// Имитируем двоичный поразрядный счетчик
Oc -> I   // 0+с=1  Счет импульса при начальном 0 дает 1 без переноса
Ic -> cO  // 1+c=c0 Счет импульса при начальной 1 дает 0 и перенос дальше
c -> I    // Перенос в самой левой позиции пораждает новый разряд
| -> c // Берем один | на счет (с) к младшему разряду

// добавить после всего выше вот это в начало
 -> &

101+11